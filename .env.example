# Supabase — project "depth-and-dawn-audio" (org: Deans Projects)
NEXT_PUBLIC_SUPABASE_URL=https://flddisogifvawenbyvln.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
# Dashboard → Project Settings → API keys → service_role (secret). Server only.
SUPABASE_SERVICE_ROLE_KEY=

# Resend — contact form delivery
RESEND_API_KEY=
RESEND_FROM_EMAIL=hello@stephaniebetschart.com
CONTACT_INBOX_EMAIL=stephaniebetschart1@gmail.com

# Shared secret for POST /api/books/sync (the Audible pipeline sends it as
# `Authorization: Bearer <token>`). Any long random string.
BOOKS_SYNC_TOKEN=

# Canonical origin, used for sitemap/robots/OG absolute URLs.
NEXT_PUBLIC_SITE_URL=https://stephaniebetschart.com
